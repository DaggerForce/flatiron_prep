def even_numbers(*num_range):
    counter = 0
    while counter <= 3:
        for num_range in range(10):
            if num_range % 2 == 0:
                if num_range == 0:
                    next
                else:
                    print(num_range)
                    counter += 1
    print(f"we have {counter} numbers")
